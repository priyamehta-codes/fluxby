meta {
  name: Parse a generic CSV file and return headers for mapping
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/import/generic/parse
  body: json
  auth: none
}

headers {
  X-Profile-Id: {{profileId}}
}

body:json {
  {}
}

docs {
  # Parse a generic CSV file and return headers for mapping
  
  No description provided.
  
  ## Endpoint
  `POST /api/import/generic/parse`
}
