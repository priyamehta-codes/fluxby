meta {
  name: Delete a recurring pattern
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/recurring/:id
  body: none
  auth: none
}

headers {
  X-Profile-Id: {{profileId}}
}

docs {
  # Delete a recurring pattern
  
  No description provided.
  
  ## Endpoint
  `DELETE /api/recurring/{id}`
}
