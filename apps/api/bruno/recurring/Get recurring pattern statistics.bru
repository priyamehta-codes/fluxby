meta {
  name: Get recurring pattern statistics
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/recurring/stats
  body: none
  auth: none
}

headers {
  X-Profile-Id: {{profileId}}
}

docs {
  # Get recurring pattern statistics
  
  No description provided.
  
  ## Endpoint
  `GET /api/recurring/stats`
}
