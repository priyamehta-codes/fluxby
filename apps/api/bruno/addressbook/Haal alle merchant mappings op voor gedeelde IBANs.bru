meta {
  name: Haal alle merchant mappings op voor gedeelde IBANs
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/addressbook/shared-iban-merchants
  body: none
  auth: none
}

headers {
  X-Profile-Id: {{profileId}}
}

docs {
  # Haal alle merchant mappings op voor gedeelde IBANs
  
  No description provided.
  
  ## Endpoint
  `GET /api/addressbook/shared-iban-merchants`
}
