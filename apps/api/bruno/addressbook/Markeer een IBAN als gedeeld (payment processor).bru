meta {
  name: Markeer een IBAN als gedeeld (payment processor)
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/addressbook/shared-ibans
  body: json
  auth: none
}

headers {
  X-Profile-Id: {{profileId}}
}

body:json {
  {}
}

docs {
  # Markeer een IBAN als gedeeld (payment processor)
  
  No description provided.
  
  ## Endpoint
  `POST /api/addressbook/shared-ibans`
}
